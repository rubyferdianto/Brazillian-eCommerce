# Apache Beam Pipeline Requirements
# Core Apache Beam with Google Cloud support
apache-beam[gcp]==2.48.0

# MongoDB dependencies
pymongo>=4.0.0

# Data processing
pandas>=1.3.0
numpy>=1.21.0

# Environment variables
python-dotenv>=0.19.0

# Google Cloud dependencies (included with apache-beam[gcp] but explicit for clarity)
google-cloud-storage>=2.0.0
google-cloud-dataflow-client>=0.8.0

# Additional utilities
typing-extensions>=4.0.0
